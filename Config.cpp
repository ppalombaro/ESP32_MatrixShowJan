#include "Config.h"

// Define the custom colors with their actual RGB values.
// This definition must appear ONLY ONCE in your entire project.
const CRGB Peach = CRGB(255, 192, 128); // The custom skin tone
const CRGB Sienna = CRGB(160, 82, 45); // A dark brown/earth tone (example value)
const CRGB RudolfBrown = CRGB(100, 50, 0); 
const CRGB RudolfTan = CRGB(180, 120, 50);
const CRGB SnowWhite = CRGB(200, 230, 255);
const CRGB CoolBlue = CRGB(190,249,249); 