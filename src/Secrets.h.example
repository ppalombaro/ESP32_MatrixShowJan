/* Secrets.h.example
   Copy this file to src/Secrets.h (which is git-ignored) and set your values.
   Config.h #includes Secrets.h; the build fails with a clear error if it is missing.
*/
#pragma once

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"
#define HOSTNAME      "palombaro-matrix"
